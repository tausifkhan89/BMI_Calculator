import 'package:flutter/material.dart';

const Color backgroundColor = Color(0xfff6f7f8);
const Color textPrimaryColor = Color(0xff151515);
const Color selectedBlueColor = Color(0xFF42a5f5);
const Color textSecondaryColor = Colors.grey;

const textStyle1 = TextStyle(fontSize: 20, color: textPrimaryColor);
const textStyle2 = TextStyle(fontSize: 20, color: textSecondaryColor);

var textColor = textSecondaryColor;

const kContainerPadding = EdgeInsets.symmetric(horizontal: 12, vertical: 6);
